## 1. 数据结构扩展

- [x] 1.1 扩展前端 `ShotData` 类型定义，新增 `DialogueData` 接口和 `dialogues` 字段
- [x] 1.2 扩展后端 `ShotSchema` 定义，新增 `dialogues` 字段支持

## 2. 前端分镜编辑器更新

- [x] 2.1 在 `JsonTableEditor.tsx` 分镜编辑区域新增台词编辑区块（位于角色标签下方）
- [x] 2.2 实现添加台词功能：角色名下拉框（从已选角色列表选择）、台词文本输入框、情感提示词输入框
- [x] 2.3 实现删除台词功能
- [x] 2.4 实现编辑台词内容功能（实时更新分镜数据）
- [x] 2.5 新增翻译键：台词、情感提示词、添加台词、删除台词等

## 3. 后端 API 实现

- [x] 3.1 新增 `character_audio` 任务类型到任务类型枚举
- [x] 3.2 新增单分镜音频生成 API：`POST /api/novels/{novel_id}/chapters/{chapter_id}/shots/{shot_index}/audio`
- [x] 3.3 新增批量章节音频生成 API：`POST /api/novels/{novel_id}/chapters/{chapter_id}/audio/generate-all`
- [x] 3.4 实现音频生成任务创建逻辑：检查角色参考音频、创建任务、构建工作流
- [x] 3.5 实现批量生成逻辑：遍历所有分镜台词、跳过无参考音频角色、记录警告
- [x] 3.6 实现任务完成后更新分镜台词的 `audio_url` 字段
- [x] 3.7 新增音频生成任务执行服务：调用 `build_audio_workflow` 并提交 ComfyUI 队列

## 4. 前端音频生成入口

- [x] 4.1 在分镜编辑区域新增"生成音频"按钮（仅当分镜有台词时显示）
- [x] 4.2 在页面工具栏新增"批量生成音频"按钮
- [x] 4.3 实现单分镜生成音频按钮点击逻辑：调用音频生成 API
- [x] 4.4 实现批量生成音频按钮点击逻辑：调用批量音频生成 API
- [x] 4.5 实现生成中状态显示和任务轮询
- [x] 4.6 实现生成失败提示和重新生成功能
- [x] 4.7 实现批量生成结果显示（成功数量、跳过警告列表）

## 5. 前端音频播放

- [x] 5.1 在台词编辑区域显示已生成音频的播放器（显示角色名称和播放按钮）
- [x] 5.2 实现音频播放功能
- [x] 5.3 实现重新生成音频功能（删除旧音频并创建新任务）

## 6. 角色库状态提示（可选）

- [x] 6.1 角色卡片显示参考音频状态（"未生成音色"或"音色已就绪"）