/**
 * 场景页面常量
 */

// 允许上传的图片类型
export const ALLOWED_IMAGE_TYPES = ['image/png', 'image/jpeg', 'image/jpg', 'image/webp'];

// 轮询配置
export const POLL_CONFIG = {
  maxAttempts: 60,
  intervalMs: 3000,
  allIntervalMs: 5000,
};
