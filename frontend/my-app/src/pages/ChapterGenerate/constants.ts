/**
 * ChapterGenerate 页面常量
 */
import { API_BASE } from '../../api';

// 导出 API_BASE 供子模块使用
export { API_BASE };

// 默认转场时长（秒）
export const DEFAULT_TRANSITION_DURATION = 2;

// 视频帧率
export const VIDEO_FPS = 25;

// 帧数计算基数（需要是 8 的倍数 +1）
export const FRAME_BASE = 8;
